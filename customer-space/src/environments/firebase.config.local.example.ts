/**
 * Modèle de config Firebase locale.
 * Copié automatiquement vers firebase.config.local.ts (gitignored) si absent,
 * ou généré via `npm run firebase:configure` / `firebase:configure:dev`.
 */
import type { FirebaseClientConfig } from './firebase-config.types';

export const firebaseConfigLocal: FirebaseClientConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
