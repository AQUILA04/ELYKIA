spring:
  application:
    name: optimize-elykia-client
  datasource:
    url: jdbc:postgresql://localhost:5432/elykiadb
    username: francis
    password: Anid2022
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket: ${MINIO_BUCKET:elykia-clients}
  public-url: ${MINIO_PUBLIC_URL:http://localhost:9000}

photo:
  fallback:
    path: ${PHOTO_FALLBACK_PATH:/opt/elykia/photos/pending}

optimize:
  client:
    auto-init:
      enabled: true
    migrate-photo:
      enabled: false
    s3-photo-migration:
      enabled: false
    info:
      "COM001":
          firstname: "Promoter"
          lastname: "Promoter type for COM001"
          phone: "1234567890"
          address: "123 Promoter St, City, Country"
          cardID: "PROMOTER-COM001"
          cardType: "CNI"
          dateOfBirth: "1990-01-01"
          collector: "collector1"
          quarter: "Q1"
          occupation: "Promoter Occupation"
          type: PROMOTER
          account:
            accountNumber: "1234567890"
            accountBalance: 100000.00

